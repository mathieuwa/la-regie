# Registre des chartes clients
Convention : la charte d'un client est enregistrée ici par `/regie onboard` (Bloc 3),
sous la forme `slug -> chemin`. À défaut, le `brand-profile.json` de
`clients/{slug}/social/` fait foi.
